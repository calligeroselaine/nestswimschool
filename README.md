# SwimNest — Homepage Concept

A single-page, front-end-only homepage concept for SwimNest, built for client review.

- `index.html` — the full concept (self-contained: fonts and hero video are embedded).
- Cinematic hero uses a sample underwater swim clip (Storyblocks preview, watermarked) as a stand-in for licensed/owned footage — replace before production launch.
- No booking system, backend, or internal pages — visual concept only.
